:normalize: (:text:)
{
    {
        # remove quotes
        match <nomultiline> (:quote:) \
                /^[[:space:]]*[[:graph:]]*>+[ \t]*[^\n]+$/ [:text:]
        alter (:quote:) /QUOTELINE/
        liaf
    }
return /:*:text:/
}